<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>My AI-Powered Shopify Store</title>

  <!-- Add custom font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

  <!-- Add custom styles -->
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f2f2f2;
    }

    .header {
      background-color: #333;
      color: white;
      padding: 15px;
      text-align: center;
    }

    .header h1 {
      margin: 0;
    }

    .header h2 {
      font-size: 18px;
      margin: 0;
      padding-top: 5px;
    }

    .header .header-content {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%;
    }

    .content {
      padding: 50px;
    }

    .product-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-gap: 30px;
    }

    .product {
      background-color: white;
      border: 1px solid #ddd;
      box-shadow: 2px 2px 5px #ddd;
      text-align: center;
      padding: 20px;
      position: relative;
    }

    .product img {
      width: 100%;
    }

    .product h3 {
      margin: 0;
    }

    .product p {
      margin: 20px 0;
    }

    .product .price {
      position: absolute;
      bottom: 20px;
      right: 20px;
      background-color: #333;
      color: white;
      padding: 10px 20px;
      border-radius: 30px;
      box-shadow: 2px 2px 5px #ddd;
    }

    .product-grid .product:hover {
      transform: scale(1.05);
      cursor: pointer;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <div class="header">
    <div class="header-content">
      <h1>My AI-Powered Shopify Store</h1>
      <h2>Experience the future of e-commerce</h2>
    </div>
  </div>

  <!-- Main content -->
  <div class="content">
    <div class="product-grid">
      <!-- Product 1 -->
      <div class="product">
        <img src="https://via.placeholder
